// .push() .unshift)
// 원본 수정됨

const numbers = [1, 2, 3, 4]
const fruits = ['Apple', 'Banana', 'Cherry']

numbers.push(5) // 맨 뒤에 삽입
console.log(numbers)

numbers.unshift(0)
console.log(numbers) // 맨 앞에 삽입
